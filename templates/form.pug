doctype html
html(lang="ja")
    head
        meta(charset="utf-8")
    body
        h1 フォーム
        form(action="/form" method="POST")
            input(type="text" name="sentence")    
            button(type="submit") 送信
        h2 分析結果
        p #{result}
